import 'package:flutter/material.dart';

class AppSizes {
  static EdgeInsets mainPadding = const EdgeInsets.symmetric(vertical: 8, horizontal: 10);
  static Radius radius = const Radius.circular(5);
}